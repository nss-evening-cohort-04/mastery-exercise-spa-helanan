(function (selectedCar) { 
	var rows = [],
	function makeRows() {
		selectedCar.forEach(function(car) {
			var row = '<div></div>';
			row.append.text(car.make);
			row.append.text(car.model);
			row.append.text(car.year);
			row.append.text(car.price);
			row.append.text(car.description);
			rows.push({
				make: make,
				model: model,
			});
		});
		}
	function appendRows() {
		var body = <td></td>
		rows.forEach(function(row) {}
	}


​var selectedCar;
  function init () {
   doStuff ();
  // code to start the application​
 } 
​
​function doStuff () {
  // Do stuff here​
}
​
​function doMoreStuff () {
 // Do more stuff here​
}
​
​// Start the application 
init (); 
}) ();
