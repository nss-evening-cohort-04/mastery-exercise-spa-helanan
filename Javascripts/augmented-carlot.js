(function (CarLot) {
	var rows = function makeRows() {
		carDescription.forEach(function(loadInventory) {
			var row = '<div></div>';
			row.append.textContent(car.make);
			row.append.textContent(car.model);
			row.append.textContent(car.year);
			row.append.textContent(car.price);
			row.append.textContent(car.description);
			rows.push({
				make: make,
				model: model,
   
      document.getElementById('selectedCar').innerHTML = newContent; }
  )};